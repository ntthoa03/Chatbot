# Cài trước khi chạy repo này:
pydantic>=2.0
pyyaml
numpy
python-dotenv

# H4-04 — document upload; ảnh/PDF scan dùng model vision API:
pypdf>=5,<7
Pillow>=10,<13
PyMuPDF>=1.24,<2

# Cho embedding/LLM — cài cả hai để hỗ trợ primary Gemini và fallback OpenAI:
openai
google-genai

# HOA-09 — giao diện test nội bộ:
streamlit>=1.40,<2

# H3-05 — HTTP adapter tạm để backend/browser gọi đúng contract tuần 1:
fastapi>=0.115,<1
uvicorn>=0.30,<1
